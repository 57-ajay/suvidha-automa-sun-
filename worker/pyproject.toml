[project]
name = "worker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "browser-use>=0.12.2",
    "patchright>=1.58.2",
    "pydantic>=2.12.5",
    "httpx>=0.28.0",
    "redis>=7.3.0",
]
